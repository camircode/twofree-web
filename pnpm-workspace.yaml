# pnpm 11 reads its settings here rather than from .npmrc.

# sharp is left unbuilt on purpose: it is Next's optional image optimiser, this
# application ships one SVG logo, and building it means pulling a prebuilt
# libvips per platform into every image build for nothing.
allowBuilds:
  esbuild: true
  sharp: false

autoInstallPeers: false

# A wildcard, not a list of exact versions. The shared packages are published
# from this project minutes before a consumer installs them, so the release-age
# policy would reject every one of them; pinning the versions here means editing
# this file on every release, and forgetting to is a build that fails on its own
# dependency for a reason nobody remembers.
minimumReleaseAgeExclude:
  - "@camircode/*"

# sharp is pinned past a published fix. It is Next's optional image optimiser and
# ships inside the standalone output, so the scan sees it in the image even
# though nothing here calls it. Trivy fails the build on HIGH or CRITICAL with a
# fix available, and this one has it.
overrides:
  sharp: 0.35.0
